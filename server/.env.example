# Server Configuration
PORT=5000
NODE_ENV=production

# MongoDB Configuration
# Obtén esto de MongoDB Atlas: https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://tomasvarelaa19_db_user:mojito2142@chapulinadb.z8uch4v.mongodb.net/?appName=chapulinadb

# JWT Configuration
# Genera un secret seguro (puedes usar: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=tu_clave_secreta_muy_segura_cambiala_en_produccion
JWT_EXPIRE=7d

# CORS Configuration
# URL de tu frontend desplegado en Vercel
CLIENT_URL=https://tu-app-frontend.vercel.app
